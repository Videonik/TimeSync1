meta:
  api_version: 2
  project_type: docker
  name: timesync
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile
  
deploy:
  environment:
    - key: PORT
      value: 3000
    - key: DB_TYPE
      value: postgres
